# Ejemplo de variables de entorno para ASTRO-Novicompu
# Copia este archivo a `.env` y ajusta los valores.
# Variables que comienzan con PUBLIC_ estarán disponibles en el navegador.

# URL base del backend Novicompu
# Desarrollo local Android Emulator (10.0.2.2 apunta al host):
PUBLIC_API_BASE=http://10.0.2.2:8004
# O si accedes directamente desde el navegador del host:
# PUBLIC_API_BASE=http://localhost:8004

